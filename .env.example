# Copy this file to .env for local runs only.
# For the GitHub Actions workflow, add HF_TOKEN as a repository secret instead:
# Settings > Secrets and variables > Actions > New repository secret
HF_TOKEN=
